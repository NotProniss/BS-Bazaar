# Example environment file for bazaar-server (do not use real secrets in this file)

# Server Configuration
PORT=3001
NODE_ENV=development

# Login Through Discord
DISCORD_CLIENT_ID=your_discord_client_id
DISCORD_CLIENT_SECRET=your_discord_client_secret
DISCORD_CALLBACK_URL=http://localhost:3001/api/auth/discord/callback

# Session and JWT secrets
# These should be strong, random strings in production
SESSION_SECRET=your_session_secret
JWT_SECRET=your_jwt_secret

# Frontend URL
# This should match the URL where your frontend is hosted
FRONTEND_URL=http://localhost:3000

# Email Configuration (Optional - for password reset and verification)
# If not configured, email features will be disabled
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_password
EMAIL_FROM=BS-Bazaar <noreply@bs-bazaar.com>

# Environment
# This should be set to 'development' for local development
# or 'production' for production environments
NODE_ENV=development
